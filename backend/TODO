Completed
- Test seeds & migrations
- Enable CORS for frontend dev
- Add basic JWT auth (dev user + protected endpoints)
- Enhanced Swagger /OpenAPI
- Added a read me file!
Next (ordered)
- Add Dockerfile + docker-compose (persist SQLite via volume)
- Add simple integration tests (login + protected endpoints)
- CI: build + test + publish image (optional)
- Cleanup & docs